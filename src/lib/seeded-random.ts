/** Deterministic 0–1 value for SSR-safe pseudo-random layouts */
export function seededRandom(seed: number): number {
  const x = Math.sin(seed * 12.9898 + 78.233) * 43758.5453;
  return x - Math.floor(x);
}
